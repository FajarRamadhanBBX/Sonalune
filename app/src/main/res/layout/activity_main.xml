<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Fragment container (for Home / Playlist / Capsule) -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_weight="1"
        android:layout_width="match_parent"
        android:layout_height="0dp" />

    <!-- Music status bar (fake Spotify style, can be extracted later to a layout file) -->
    <LinearLayout
        android:id="@+id/music_status_bar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#222"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- Fake status info -->
        <TextView
            android:id="@+id/tv_song_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Now Playing: Lo-Fi Chill"
            android:textColor="#fff"/>

        <!-- Play/Pause icon -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_lock"
            app:tint="#fff" />
    </LinearLayout>

    <com.sonalune.pbp.view.ui_components.NavBar
        android:id="@+id/navbar_component"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>
